# AI module dependencies (Version 2 — training infrastructure).
# Separate from the backend's requirements.txt on purpose: the MVP
# runs without any of these installed.
#
# Install:  pip install -r ai/requirements.txt

ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0
opencv-python>=4.9.0
numpy>=1.26.0
matplotlib>=3.8.0
pyyaml>=6.0

# Training telemetry + model export/serving (Phases 8D/8E).
# onnxruntime is also what perception/detectors/yolo.py uses at
# backend runtime when perception_detector = "yolo"; the backend
# degrades gracefully to ground_truth when it is not installed.
tensorboard>=2.16.0
onnx>=1.16.0
onnxruntime>=1.18.0
